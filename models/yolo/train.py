def train_single_stage(model_name, data_path, epochs, batch_size, device):
    print(f"[INFO] Training {model_name} for single-stage task")
    # Insert YOLO training logic here, for example:
   # from ultralytics import YOLO
   # model = YOLO(model_name)
   # model.train(data=data_path, epochs=epochs, batch=batch_size, device=device)
   # model.val()